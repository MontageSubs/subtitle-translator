name: Status Admin Ops

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Operation"
        required: true
        type: choice
        options:
          - trigger_cycle
          - purge_recent
          - delete_snapshot
          - upsert_snapshot
        default: trigger_cycle
      days:
        description: "purge_recent: delete data newer than N days"
        required: false
        default: "1"
      date:
        description: "delete_snapshot / upsert_snapshot: target date (YYYY-MM-DD)"
        required: false
      component_id:
        description: "delete_snapshot (optional = whole day) / upsert_snapshot (required)"
        required: false
      status:
        description: "upsert_snapshot: operational | degraded | outage | nodata"
        required: false
        type: choice
        options:
          - operational
          - degraded
          - outage
          - nodata
        default: operational
      uptime_ratio:
        description: "upsert_snapshot: 0-100, defaults based on status if left blank"
        required: false

permissions:
  contents: read

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve admin base URL
        id: resolve
        env:
          STATUS_WORKER_URL: ${{ vars.STATUS_WORKER_URL }}
        run: |
          if [ -z "$STATUS_WORKER_URL" ]; then
            echo "STATUS_WORKER_URL is not configured as a repository/organization variable." >&2
            exit 1
          fi
          echo "url=${STATUS_WORKER_URL%/}/api/admin" >> "$GITHUB_OUTPUT"

      - name: trigger_cycle
        if: ${{ inputs.action == 'trigger_cycle' }}
        env:
          TOKEN: ${{ secrets.STATUS_ADMIN_TOKEN }}
        run: |
          resp=$(curl -sS -w "\n%{http_code}" -X POST "${{ steps.resolve.outputs.url }}/cycle/trigger" \
            -A "Status-Admin-Ops/1.0" \
            -H "X-Gateway-Automation-Token: ${TOKEN}")
          body=$(echo "$resp" | head -n -1)
          code=$(echo "$resp" | tail -n 1)
          echo "HTTP Code: $code"
          echo "Response: $body"
          if [ "$code" -lt 200 ] || [ "$code" -ge 300 ]; then
            exit 1
          fi

      - name: purge_recent
        if: ${{ inputs.action == 'purge_recent' }}
        env:
          TOKEN: ${{ secrets.STATUS_ADMIN_TOKEN }}
          DAYS: ${{ inputs.days }}
        run: |
          resp=$(curl -sS -w "\n%{http_code}" -X POST "${{ steps.resolve.outputs.url }}/data/purge" \
            -A "Status-Admin-Ops/1.0" \
            -H "X-Gateway-Automation-Token: ${TOKEN}" \
            -H "Content-Type: application/json" \
            -d "{\"days\": ${DAYS}}")
          body=$(echo "$resp" | head -n -1)
          code=$(echo "$resp" | tail -n 1)
          echo "HTTP Code: $code"
          echo "Response: $body"
          if [ "$code" -lt 200 ] || [ "$code" -ge 300 ]; then
            exit 1
          fi

      - name: delete_snapshot
        if: ${{ inputs.action == 'delete_snapshot' }}
        env:
          TOKEN: ${{ secrets.STATUS_ADMIN_TOKEN }}
          DATE: ${{ inputs.date }}
          COMPONENT_ID: ${{ inputs.component_id }}
        run: |
          if [ -z "$DATE" ]; then
            echo "date is required for delete_snapshot" >&2
            exit 1
          fi
          if [ -n "$COMPONENT_ID" ]; then
            body_payload="{\"date\": \"${DATE}\", \"componentId\": \"${COMPONENT_ID}\"}"
          else
            body_payload="{\"date\": \"${DATE}\"}"
          fi
          resp=$(curl -sS -w "\n%{http_code}" -X DELETE "${{ steps.resolve.outputs.url }}/snapshots" \
            -A "Status-Admin-Ops/1.0" \
            -H "X-Gateway-Automation-Token: ${TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$body_payload")
          body=$(echo "$resp" | head -n -1)
          code=$(echo "$resp" | tail -n 1)
          echo "HTTP Code: $code"
          echo "Response: $body"
          if [ "$code" -lt 200 ] || [ "$code" -ge 300 ]; then
            exit 1
          fi

      - name: upsert_snapshot
        if: ${{ inputs.action == 'upsert_snapshot' }}
        env:
          TOKEN: ${{ secrets.STATUS_ADMIN_TOKEN }}
          DATE: ${{ inputs.date }}
          COMPONENT_ID: ${{ inputs.component_id }}
          STATUS: ${{ inputs.status }}
          UPTIME_RATIO: ${{ inputs.uptime_ratio }}
        run: |
          if [ -z "$DATE" ] || [ -z "$COMPONENT_ID" ]; then
            echo "date and component_id are required for upsert_snapshot" >&2
            exit 1
          fi
          if [ -n "$UPTIME_RATIO" ]; then
            body_payload="{\"date\": \"${DATE}\", \"componentId\": \"${COMPONENT_ID}\", \"status\": \"${STATUS}\", \"uptimeRatio\": ${UPTIME_RATIO}}"
          else
            body_payload="{\"date\": \"${DATE}\", \"componentId\": \"${COMPONENT_ID}\", \"status\": \"${STATUS}\"}"
          fi
          resp=$(curl -sS -w "\n%{http_code}" -X PUT "${{ steps.resolve.outputs.url }}/snapshots" \
            -A "Status-Admin-Ops/1.0" \
            -H "X-Gateway-Automation-Token: ${TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$body_payload")
          body=$(echo "$resp" | head -n -1)
          code=$(echo "$resp" | tail -n 1)
          echo "HTTP Code: $code"
          echo "Response: $body"
          if [ "$code" -lt 200 ] || [ "$code" -ge 300 ]; then
            exit 1
          fi
